# Jmp SPA Yeoman Generator

[Yeoman](https://yeoman.io/) generator for my basic single page web application
project template. It uses Webpack to smoosh everything into a single HTML file.

`yo jmp-spa`

